name: ai-bot

services:
  app:
    image: ${AI_BOT_IMAGE:-ai-bot:latest}
    container_name: ai-bot
    restart: unless-stopped
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/app/data
